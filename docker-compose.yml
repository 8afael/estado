services:

  api:
    build:
      context: .
      dockerfile: Dockerfile.api

    volumes:
      - ./data:/data

    ports:
      - "8500:8500"

    environment:
      - PYTHONUNBUFFERED=1

  # dashboard:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.streamlit

  #   volumes:
  #     - ./data:/data

  #   ports:
  #     - "8501:8501"

  #   depends_on:
  #     - api
